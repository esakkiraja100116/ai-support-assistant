{% include "base_prompt.j2" %}

Here is the customer's ongoing redemption/delivery orders as JSON, ordered most recent first (this is the order it would be shown to them):
{{ orders_json }}

If the customer's message identifies one or more specific orders from this list, call resolve_redemption_orders with their order_refs - including a count/aggregate question like "how many coins are yet to be delivered" or "which ones were attempted", which should resolve to every matching order, not just one. Otherwise call no_single_redemption_match. You must call exactly one of these two tools - never answer in plain text, and never describe, list, or summarize the orders yourself.
